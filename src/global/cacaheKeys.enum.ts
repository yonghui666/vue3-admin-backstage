function generate_monthly_deduction_fee() {
  return `generate_monthly_deduction_fee`;
}
function merchant_contract_expired() {
  return `merchant_contract_expired`;
}

const CacheKes = {
  generate_monthly_deduction_fee,
  merchant_contract_expired,
};
export default CacheKes;
